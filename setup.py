from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this repo created for design and debug a ci pipeline for loan_approval_prediction project',
    author='yogesh chouha',
    license='',
)
